{
  "name": "similartube",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:extension": "npm run dev --workspace=apps/extension"
  },
  "devDependencies": {
    "tsx": "^4.23.12"
  }
}
